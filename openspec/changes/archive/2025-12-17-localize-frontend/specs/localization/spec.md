## ADDED Requirements
### Requirement: Chinese Language Interface
The system SHALL provide the user interface in Simplified Chinese (zh-CN) by default.

#### Scenario: Default Language
- **WHEN** a user visits the site
- **THEN** the language attribute of the HTML tag SHALL be `zh-CN`
- **AND** all static UI text SHALL be in Simplified Chinese

